version: "3.8"

services:
  code-reviewer-app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    ports:
      - "8000:8000"
    restart: unless-stopped
    volumes:
      - ./.env:/app/.env # Make .env updates visible to container
